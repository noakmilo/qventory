# DATABASE
# Development: Use PostgreSQL local or SQLite
# Production: Use your production PostgreSQL URL
DATABASE_URL=postgresql://qventory_user:password@localhost:5432/qventory_db

# Flask
FLASK_APP=run.py
FLASK_ENV=production
FLASK_DEBUG=0
SECRET_KEY=CHANGE-THIS-TO-A-RANDOM-SECRET-KEY

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# eBay API
# Get credentials from: https://developer.ebay.com/my/keys
EBAY_ENV=production
EBAY_CLIENT_ID=your-ebay-client-id
EBAY_CLIENT_SECRET=your-ebay-client-secret
EBAY_REDIRECT_URI=https://yourdomain.com/ebay/callback

# Cloudinary (Image hosting)
# Get credentials from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# OpenAI API (for AI-powered receipt OCR)
# Get API key from: https://platform.openai.com/api-keys
# Required for GPT-4 Vision receipt processing
# Cost: ~$0.01-0.03 per receipt image
OPENAI_API_KEY=your-openai-api-key
OCR_PROVIDER=openai_vision

# Admin Password (for admin features)
ADMIN_PASSWORD=your-secure-admin-password

# Shippo (Tracking API)
# Get API key from: https://goshippo.com/
SHIPPO_API_KEY=your-shippo-api-key

# Email / SMTP Configuration
# REQUIRED for email verification and password reset
# Choose one of these providers:

# Option 1: Gmail (for testing/small deployments)
# - Enable 2FA on your Google account
# - Generate App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@qventory.com
SMTP_PASSWORD=ueottehhckzzgphq

# Option 2: SendGrid (recommended for production)
# - Sign up at https://sendgrid.com
# - Create API key in Settings > API Keys
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key

# Option 3: AWS SES (good for high volume)
# - Verify domain in AWS SES console
# - Create SMTP credentials
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=your-aws-smtp-username
# SMTP_PASSWORD=your-aws-smtp-password

# Option 4: Mailgun
# - Sign up at https://mailgun.com
# - Get SMTP credentials from domain settings
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@your-domain.mailgun.org
# SMTP_PASSWORD=your-mailgun-smtp-password

# Optional: Custom sender configuration
# SMTP_FROM_EMAIL=noreply@yourdomain.com
# SMTP_FROM_NAME=Qventory

# Security - MUST be True in production with HTTPS
SESSION_COOKIE_SECURE=True
REMEMBER_COOKIE_SECURE=True

# Server
PORT=5000
